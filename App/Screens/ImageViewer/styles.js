import { StyleSheet } from "react-native";
import CommonStyles from "../../Utils/Theme/CommonStyles";

export default StyleSheet.create({
    title: {
        fontWeight: 'bold'
      },
    caption: {
        fontWeight: 'normal'
    }
})